[project]
name = "teachclaw_scrubbed_proof_loop"
version = "0.1.0"
description = "Public-safe TeachClaw workflow proof-loop example"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
